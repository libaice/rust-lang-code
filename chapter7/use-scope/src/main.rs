// use std::collections::HashMap;


use std::fmt::Result;
use std::io::Result as IoResult;

fn main() {

    // 函数引入到 父级作用域

    // struct enum   本身
    // let mut map = HashMap::new();
    // map.insert(1, 3);
}

